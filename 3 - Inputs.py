print("Hello " + input("Enter your name: ") + "!")
# The above code takes user input for their name and prints "Hello [name]!" where [name] is the input provided by the user.
# The input function is used to take input from the user.
# The input function returns a string, so we can concatenate it with other strings.
# The input function can also take a prompt string as an argument, which is displayed to the user.

